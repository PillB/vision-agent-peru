# Agent Requests

This rolling log preserves up to the last 10 user requests available to the agent. Older entries should be removed when an eleventh request is added.

## 2026-08-09 — Model coverage, visual validation, tracking graph, research, and deployment

Focus on not-yet-implemented model endpoints so every use case can select all advertised models. Exercise every toggle and relevant toggle combination in the Prototype and Evidence Workspace ("sandbox") with Playwright screenshots; test every use case and verify required detections and readable, scaled, overlap-aware annotations. Research best practices for computer-vision visualization, annotation placement, tracking, clustering, subject re-identification, and competitor/research systems. Show how many times and how long a subject appears; build a graph that uniquely tracks people and objects within the permitted privacy boundary and weights relationships using co-occurrence count, duration, proximity, and related metrics. Test webcam owner enrollment and verification while avoiding identification or automatic enrollment of bystanders. Diagnose, compare, rank, calibrate, optimize, integrate, and validate model/VLM alternatives; examine downstream impacts and preempt regressions. Complete four change iterations and five optimization iterations, then commit, merge, deploy, verify the live site and GitHub/local parity. Save the task here and retain the last 10 user messages.

Source note: normalized from the current pasted request; spelling was cleaned without narrowing its scope.
